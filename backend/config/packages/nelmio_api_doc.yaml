nelmio_api_doc:
  documentation:
    info:
      title: Gestion Bibliotheque API
      description: API REST pour la gestion de bibliotheque
      version: 1.0.0
    components:
      securitySchemes:
        bearerAuth:
          type: http
          scheme: bearer
          bearerFormat: JWT
    security:
      - bearerAuth: []
  areas:
    path_patterns:
      - ^/api(?!/doc$)
